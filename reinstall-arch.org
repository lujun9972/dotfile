#+TITLE: 装Arch记
#+AUTHOR: lujun9972
#+CATEGORY: dotfile
#+DATE: [2026-05-02 六 08:00]
#+OPTIONS: ^:{}

* 说明

本文档最早编写于 2016 年，记录了我每次重装 Arch Linux 后需要做的配置。随着时间推移，很多软件包和配置方式已经发生了变化，现整理成一份当前可用的版本。

Arch Linux 基本系统的安装步骤（分区、格式化、pacstrap、arch-chroot 等）推荐直接参考官方 Wiki：
[[https://wiki.archlinux.org/title/Installation_guide][Arch Wiki 安装指南]]

本文档从 arch-chroot 之后的步骤开始。

* 配置网络

#+BEGIN_SRC sh :dir /sudo::
  pacman --noconfirm -S iwd
  systemctl enable iwd
#+END_SRC

若在桌面环境使用，也可选择 NetworkManager：
#+BEGIN_SRC sh :dir /sudo::
  pacman --noconfirm -S networkmanager
  systemctl enable NetworkManager
#+END_SRC

* 配置用户

1. 创建用户
   #+BEGIN_SRC sh :dir /sudo::
     useradd -m -g users -s /bin/bash lujun9972
   #+END_SRC

2. 设置密码并加入 wheel 组
   #+BEGIN_SRC sh :dir /sudo::
     passwd lujun9972
     usermod -a -G wheel lujun9972
   #+END_SRC

3. 配置 sudo，让 wheel 组可以使用 sudo
   #+BEGIN_SRC sh :dir /sudo::
     visudo
   #+END_SRC
   取消 =%wheel ALL=(ALL:ALL) ALL= 的注释

* 添加 Arch Linux CN 镜像源

#+BEGIN_SRC shell :dir /sudo::
  cat >>/etc/pacman.conf <<EOF
  [archlinuxcn]
  SigLevel = Optional TrustedOnly
  Server = https://mirrors.ustc.edu.cn/archlinuxcn/\$arch
  EOF

  pacman -Syy
  pacman --noconfirm -S archlinuxcn-keyring
#+END_SRC

* 安装 yay (AUR 助手)

#+BEGIN_SRC sh :dir /sudo::
  pacman --noconfirm -S yay
#+END_SRC

* 基础软件包

#+BEGIN_SRC sh :dir /sudo::
  pacman --noconfirm -S \
    git openssh pandoc \
    bash-completion \
    zip unzip unrar ntfs-3g \
    cmake gcc \
    aspell aspell-en \
    mailutils \
    intel-ucode
#+END_SRC

* 桌面环境

** Xorg

#+BEGIN_SRC sh :dir /sudo::
  pacman --noconfirm -S xorg xorg-server xorg-xinit
  # 可选：触摸板驱动
  # pacman -S xf86-input-synaptics
#+END_SRC

** 字体

#+BEGIN_SRC sh :dir /sudo::
  pacman --noconfirm -S ttf-dejavu wqy-microhei
#+END_SRC

** Awesome WM

#+BEGIN_SRC sh :dir /sudo::
  pacman --noconfirm -S awesome archlinux-xdg-menu
#+END_SRC

在 =~/.xinitrc= 中添加：
#+BEGIN_SRC conf
  exec awesome
#+END_SRC

生成 XDG 菜单：
#+BEGIN_SRC sh
  mkdir -p ~/.config/awesome/
  xdg_menu --format awesome --root-menu /etc/xdg/menus/arch-applications.menu >~/.config/awesome/archmenu.lua
#+END_SRC

** 中文输入法 (fcitx5)

#+BEGIN_SRC sh :dir /sudo::
  pacman --noconfirm -S fcitx5-im fcitx5-chinese-addons
#+END_SRC

在 =~/.xinitrc= 中添加：
#+BEGIN_SRC conf
  export GTK_IM_MODULE=fcitx5
  export QT_IM_MODULE=fcitx5
  export XMODIFIERS="@im=fcitx5"
  fcitx5 &
#+END_SRC

* 声音配置

#+BEGIN_SRC sh :dir /sudo::
  pacman --noconfirm -S alsa-utils pulseaudio-alsa
#+END_SRC

Arch Linux 默认静音，用 =alsamixer= 取消 Master 和 PCM 的静音（按 M 键）。

* 网络工具

** ShadowSocks

#+BEGIN_SRC sh :dir /sudo::
  pacman --noconfirm -S shadowsocks-rust
#+END_SRC

可选 GUI 客户端：
#+BEGIN_SRC sh :dir /sudo::
  pacman --noconfirm -S v2ray-desktop
#+END_SRC

** 浏览器

#+BEGIN_SRC sh :dir /sudo::
  pacman --noconfirm -S w3m imlib2
#+END_SRC

* 开发工具

** Emacs + Spacemacs

#+BEGIN_SRC sh :dir /sudo::
  pacman --noconfirm -S emacs
#+END_SRC

#+BEGIN_SRC sh
  git clone git@github.com:lujun9972/bin.git ~/bin
  git clone git@github.com:lujun9972/MyLisp.git ~/MyLisp
  git clone git@github.com:lujun9972/.spacemacs.d.git ~/.spacemacs.d
  mv ~/.emacs.d ~/emacs.d.bak
  git clone https://github.com/syl20bnr/spacemacs ~/.emacs.d
#+END_SRC

Spacemacs 的 python layer 需要 virtualenvwrapper（已从 Arch 仓库移除，通过 pip 安装）：
#+BEGIN_SRC shell :dir /sudo::
  pacman --noconfirm -S python-pip python-virtualenv
  pip install virtualenvwrapper
#+END_SRC

* 终端工具

** 终端模拟器

#+BEGIN_SRC sh :dir /sudo::
  pacman --noconfirm -S terminator rxvt-unicode
#+END_SRC

** 文件管理器

轻量级文件管理器 pcmanfm（若主要在 Emacs 中使用 dired，可跳过）：
#+BEGIN_SRC sh :dir /sudo::
  pacman --noconfirm -S pcmanfm
#+END_SRC

** 看图与截图

- TTY 下：fbida (看图/PDF)、fbgrab (截图)
- X/Wayland 下：feh/sxiv (看图)、zathura (PDF)、scrot (截图)

#+BEGIN_SRC sh :dir /sudo::
  pacman --noconfirm -S feh sxiv zathura zathura-pdf-poppler
  # TTY 工具（可选）：
  # pacman --noconfirm -S fbida fbgrab
#+END_SRC

** 终端中文环境 (TTY)

若需要在纯 TTY 环境下显示和输入中文，fbterm 已从官方仓库移除，需从 AUR 安装：
#+BEGIN_SRC sh :dir /sudo:: :var YOUR_USERNAME=(user-login-name)
  yay -S fbterm libx86
  gpasswd -a $YOUR_USERNAME video
  sudo setcap 'cap_sys_tty_config+ep' /usr/bin/fbterm
#+END_SRC

中文输入法：
#+BEGIN_SRC sh :dir /sudo::
  yay -S fcitx-fbterm-git
#+END_SRC

启动方式：=fcitx-fbterm-helper -l=

** 媒体播放

#+BEGIN_SRC sh :dir /sudo::
  pacman --noconfirm -S mpv mplayer mpg123 cmus
#+END_SRC

** 网易云音乐

命令行版：
#+BEGIN_SRC sh :dir /sudo::
  pacman --noconfirm -S go-musicfox-git
#+END_SRC

GTK4 图形界面版：
#+BEGIN_SRC sh :dir /sudo::
  pacman --noconfirm -S netease-cloud-music-gtk4
#+END_SRC

** RSS 阅读器

#+BEGIN_SRC sh :dir /sudo::
  pacman --noconfirm -S newsboat
#+END_SRC

** 下载工具

#+BEGIN_SRC sh :dir /sudo::
  pacman --noconfirm -S aria2 rtorrent
#+END_SRC

** 日历与日程

#+BEGIN_SRC sh :dir /sudo::
  pacman --noconfirm -S calcurse remind
#+END_SRC

** 记账

Ledger 已在官方 extra 源：
#+BEGIN_SRC sh :dir /sudo::
  pacman --noconfirm -S ledger
#+END_SRC

** 其他实用工具

#+BEGIN_SRC sh :dir /sudo::
  pacman --noconfirm -S zeal pv toilet
#+END_SRC

* 可选应用

** 坚果云 (AUR)

#+BEGIN_SRC sh :dir /sudo::
  yay --noconfirm -S nutstore
#+END_SRC

** WPS Office

#+BEGIN_SRC sh :dir /sudo::
  pacman --noconfirm -S wps-office ttf-wps-fonts
#+END_SRC

** 终端 IRC 客户端

#+BEGIN_SRC sh :dir /sudo::
  pacman --noconfirm -S weechat
#+END_SRC

* 恢复 dotfile 配置

我的配置文件全部以 Org-mode src block 的形式托管在 GitHub 上，只需 tangl 即可恢复：

#+BEGIN_SRC emacs-lisp :results raw
  (require 'url-handlers)
  (let ((tmpfile (make-temp-name "/tmp/dotfile")))
    (url-copy-file "https://raw.githubusercontent.com/lujun9972/dotfile/master/dotfile.org" tmpfile)
    (find-file tmpfile)
    (org-mode)
    (call-interactively #'org-babel-tangle)
    (delete-file tmpfile))
#+END_SRC
